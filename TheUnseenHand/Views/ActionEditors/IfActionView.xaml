<UserControl x:Class="TheUnseenHand.Views.ActionEditors.IfActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1.35*"/>
            </Grid.ColumnDefinitions>

            <ComboBox ItemsSource="{Binding Sources}"
                      SelectedItem="{Binding Source}"/>
            <ComboBox Grid.Column="2"
                      ItemsSource="{Binding Operators}"
                      SelectedItem="{Binding Operator}"/>
            <TextBox Grid.Column="4"
                     Text="{Binding ComparisonValue, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Actions}"
                 SelectedItem="{Binding SelectedAction}"
                 DisplayMemberPath="DisplayText"
                 Background="#22000000"
                 Foreground="White"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Add" Width="70" Margin="0,0,8,0" Click="AddButton_Click"/>
            <Button Content="Edit" Width="70" Margin="0,0,8,0" Click="EditButton_Click"/>
            <Button Content="Remove" Width="70" Margin="0,0,8,0" Click="RemoveButton_Click"/>
            <Button Content="▲" Width="32" Margin="0,0,6,0" Click="MoveUpButton_Click"/>
            <Button Content="▼" Width="32" Click="MoveDownButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
