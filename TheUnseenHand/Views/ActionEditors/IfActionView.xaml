<UserControl x:Class="TheUnseenHand.Views.ActionEditors.IfActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1.35*"/>
            </Grid.ColumnDefinitions>
            <ComboBox ItemsSource="{Binding Sources}" SelectedItem="{Binding Source}"/>
            <ComboBox Grid.Column="2" ItemsSource="{Binding Operators}"
                      SelectedItem="{Binding Operator}"/>
            <TextBox Grid.Column="4"
                     Text="{Binding ComparisonValue, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMobCondition}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <Grid Grid.Row="1" Grid.ColumnSpan="5" Margin="0,8,0,0">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMobCondition}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="64"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding NewMobName, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Content="Add" Click="AddMobButton_Click"/>
                <ListBox Grid.Row="1" Grid.ColumnSpan="2" Margin="0,5,0,0"
                         ItemsSource="{Binding MobNames}"
                         SelectedItem="{Binding SelectedMobName}"
                         Background="#22000000" Foreground="White"/>
                <Button Grid.Row="1" Grid.Column="2" Margin="0,5,0,0"
                        Content="Remove" Click="RemoveMobButton_Click"/>
            </Grid>
        </Grid>

        <GroupBox Grid.Row="2" Header="THEN" Foreground="White">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox ItemsSource="{Binding Actions}"
                         SelectedItem="{Binding SelectedAction}"
                         DisplayMemberPath="DisplayText"
                         Background="#22000000" Foreground="White"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="Add" Width="58" Margin="0,0,5,0" Click="AddButton_Click"/>
                    <Button Content="Edit" Width="58" Margin="0,0,5,0" Click="EditButton_Click"/>
                    <Button Content="Remove" Width="64" Margin="0,0,5,0" Click="RemoveButton_Click"/>
                    <Button Content="▲" Width="30" Margin="0,0,5,0" Click="MoveUpButton_Click"/>
                    <Button Content="▼" Width="30" Click="MoveDownButton_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="ELSE" Foreground="White">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox ItemsSource="{Binding ElseActions}"
                         SelectedItem="{Binding SelectedElseAction}"
                         DisplayMemberPath="DisplayText"
                         Background="#22000000" Foreground="White"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="Add" Width="58" Margin="0,0,5,0" Click="AddElseButton_Click"/>
                    <Button Content="Edit" Width="58" Margin="0,0,5,0" Click="EditElseButton_Click"/>
                    <Button Content="Remove" Width="64" Margin="0,0,5,0" Click="RemoveElseButton_Click"/>
                    <Button Content="▲" Width="30" Margin="0,0,5,0" Click="MoveElseUpButton_Click"/>
                    <Button Content="▼" Width="30" Click="MoveElseDownButton_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
