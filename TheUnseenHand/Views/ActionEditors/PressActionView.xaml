<UserControl x:Class="TheUnseenHand.Views.ActionEditors.PressActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Key:" VerticalAlignment="Center" />
        <TextBox Grid.Column="2"
                 Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Hold for (ms):" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="2"
                 Text="{Binding DurationMilliseconds, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>